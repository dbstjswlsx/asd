export default function BottomNav({screen,setScreen}:{screen:string;setScreen:(s:string)=>void}) { const items=[['home','⌖','홈'],['market','▦','코스 마켓'],['groups','◎','모임 지도'],['stamps','★','스탬프'],['ranking','♕','랭킹'],['profile','◉','내 프로필']]; return <nav className="bottom-nav">{items.map(([id,icon,label])=><button key={id} className={screen===id?'selected':''} onClick={()=>setScreen(id)}><b>{icon}</b><span>{label}</span></button>)}</nav> }
